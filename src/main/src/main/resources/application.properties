# --- Spring Boot datasource (for app logic, runs locally) ---
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# --- Flyway migration datasource (runs on remote DB) ---
# spring.flyway.url=${SPRING_FLYWAY_URL}
# spring.flyway.user=${SPRING_FLYWAY_USER}
# spring.flyway.password=${SPRING_FLYWAY_PASSWORD}

# Optional: show Flyway migration info in logs
spring.flyway.locations=classpath:db/migration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true